<div class="row">
  <div class='col-md-1' ><span class='<%= "alert-danger" if person.errors.details.keys.include?(:name) %>'  style="display: inherit;"  >  <%= f.text_field :name, value: @person.name, autocomplete: 'off', size: 40, class: 'form-control', type: '' %>
   <label class='form-text' >Name</label>
</span></div>

  <div class="col-md-2">
    <%= link_to "Cancel", admin_account_persons_path,  {class: "btn btn-secondary"} %>
    <%= f.submit "Save", class: "btn btn-primary pull-right" %>
  </div>
</div>
